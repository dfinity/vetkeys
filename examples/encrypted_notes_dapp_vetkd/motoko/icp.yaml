canisters:
  - name: encrypted_notes
    recipe:
      type: "@dfinity/motoko@v4.1.0"
      configuration:
        main: backend/main.mo
    init_args:
      type: text
      value: "(\"test_key_1\")"

  - name: www
    recipe:
      type: "@dfinity/asset-canister@v2.1.0"
      configuration:
        dir: dist
        build:
          - cd frontend && npm i --include=dev && npm run build && cd - && rm -rf dist; cp -r frontend/dist ./

networks:
  - name: local
    mode: managed
    ii: true
